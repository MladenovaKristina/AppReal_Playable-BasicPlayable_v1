import ConfigurableParams from '../../../data/configurable_params';
import { Rectangle, DisplayObject, TextField, Tween, Ease } from '../../../utils/black-engine.module';

export default class ProgressBars extends DisplayObject {
  constructor(text) {
    super();

    
  }

  onAdded() {
    console.log("I will be a progress bar");
    
  }
}
